# ================================
# TOTAL GRIND - Variables de Entorno
# ================================
# Copia este archivo a .env y configura los valores

# ===== REQUERIDO =====
# Secreto para firmar tokens JWT (OBLIGATORIO en producción)
# Genera uno con: openssl rand -base64 32
JWT_SECRET=

# ===== MONGODB (opcional - tienen valores por defecto) =====
MONGO_USER=mernuser
MONGO_PASSWORD=cambia-esta-contraseña-en-produccion

# ===== NOTAS =====
# - JWT_SECRET es OBLIGATORIO, docker compose fallará sin él
# - En el VPS, exporta las variables antes de ejecutar docker compose:
#   export JWT_SECRET=$(openssl rand -base64 32)
#   export MONGO_PASSWORD=$(openssl rand -base64 16)
#   docker compose up -d
